# example: main_lib = {episode_code: [episode_text, info_buttons("list of
# information for the buttons, len(info_buttons) / 2 is number of buttons"),
# jump_episode_code("list of ways to delta episode_code, length == length info_buttons")]}

episode_position = '1'

main_lib = {}
